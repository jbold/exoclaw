[package]
name = "mock-channel"
version = "0.1.0"
edition = "2024"
description = "Mock channel adapter plugin for testing the webhook pipeline"

[lib]
crate-type = ["cdylib"]

[dependencies]
extism-pdk = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[workspace]
