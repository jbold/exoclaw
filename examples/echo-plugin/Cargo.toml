[package]
name = "echo-plugin"
version = "0.1.0"
edition = "2024"
description = "Minimal exoclaw plugin that echoes messages back"

[lib]
crate-type = ["cdylib"]

[dependencies]
extism-pdk = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
